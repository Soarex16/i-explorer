package com.soarex16.iexporer.model

enum class MethodVisibility(visibility: String) {
    PUBLIC("public"),
    PRIVATE("private"),
    PROTECTED("protected"),
    PACKAGE_PRIVATE("packagePrivate")
}